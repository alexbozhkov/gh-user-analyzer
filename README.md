# GitHub User Analyzer Solution

## Overview
This solution shows an optimal implementation with `GraphQL` and a sub-optimal one with basic `REST`.

1. Requirements
- docker v29+ (with compose v5+)
- make

2. Simple run:
```bash
docker compose up --build
```

3. (Optional) Build and run the project with:
```bash
make build
```
